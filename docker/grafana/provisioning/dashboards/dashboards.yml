apiVersion: 1
disableDeletion: true
folders:
  - name: MikroTik
    folderUid: mikrotik-folder
    provider:
      name: mikrotik-dashboards
      type: file
      disableDeletion: false
      updateIntervalSeconds: 30
      options:
        path: /etc/grafana/provisioning/dashboards
